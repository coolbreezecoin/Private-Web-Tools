---
import BaseLayout from "@/layouts/BaseLayout.astro";
import { defaultLocale } from "@/i18n/config";
import { t as getStrings } from "@/i18n/strings";

const locale = defaultLocale;
const basePath = "/about";
const t = getStrings(locale).pages.about;
---

<BaseLayout title={t.title} description={t.description} basePath={basePath} locale={locale}>
  <main class="page-shell section prose">
    <h1>{t.h1}</h1>
    <p>{t.intro}</p>
    <h2>{t.privateTitle}</h2>
    <p>{t.privateBody}</p>
    <h2>{t.useTitle}</h2>
    <p>{t.useBody}</p>
  </main>
</BaseLayout>
